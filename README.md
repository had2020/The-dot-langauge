# The-dot-langauge

Note: I created this project as a joke. Playing with the idea of actually trying to suck up to Calculus, and allow for floating point like numbers that can be infinity big. All of this runs a in a virtual machine. The text below is something I wrote while making the project as a joke, with at 3am when I decided to make this project.

This in language is very human reable and desmstractres the oppistie of optimization, pointer indictrioning to the max, only memory in ram, no l3-l1s chache, base 3 align,  only analog heap float, ram costs are only NAND storage and no ramfs, only single threadedness for human understablity. how needs arrays only matrixs in 2Ds for AI. No match/switch statemnets or Jumps tables, for human readablily only firect branchs. No comments for compiler optimization as we need all we can get. And to make sure the language is better for calulus math not discrete, as discrete human readable and not natural. No libaries, or package managers as you could get viruses. You also get the gift of unlimited free, register at the low cost of no direct register access. To save people fighting the debugger you code will never debug, and will allow for bugs, to make sure your state of the art code is running. JOT, just out of time compiled. This code only runs in x86 as reduced ISA like (RISC) are not human readable enough.  

# The-dot-langauge

This in language is very human reable and desmstractres the oppistie of optimization, pointer indictrioning to the max, only memory in ram, no l3-l1s chache, base 3 align,  only analog heap float, ram costs are only NAND storage and no ramfs, only single threadedness for human understablity. how needs arrays only matrixs in 2Ds for AI. No match/switch statemnets or Jumps tables, for human readablily only firect branchs. No comments for compiler optimization as we need all we can get. And to make sure the language is better for calulus math not discrete, as discrete human readable and not natural. No libaries, or package managers as you could get viruses. You also get the gift of unlimited free, register at the low cost of no direct register access. To save people fighting the debugger we won't have one, instead we have ramdom evolving codes that self curpport, to make sure your state of the art code is running. JOT, just out of time compiled. This code only runs in x86 as reduced ISA like (RISC) are not human readable enough.  
